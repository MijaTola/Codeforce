#include <bits/stdc++.h>

using namespace std;

int v[100010];
int main() {
    
    int n,m;
    cin >> n >> m;
    
    int c = 0;
    int mx = 0;
    int total = 0;
    for (int i = 0; i < n; ++i) 
        cin >> v[i];

    sort(v, v + n);

    for (int i = 0; i < n; ++i) {
    
    }
    
    
    return 0;
}

