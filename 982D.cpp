#include <bits/stdc++.h>

using namespace std;

int v[100010];
int n;
int ver(int k) {
    
}
int main(){
    cin >> n;
    for (int i = 0; i < n; ++i)
        cin >> v[i];

    int a = -1; 
    int b = 1e9+1;
    while(b - a > 1) {
        int mid = (a + b) / 2;

    }
    return 0;
}

