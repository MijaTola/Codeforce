#include <bits/stdc++.h>

using namespace std;

int n,m,k;
int v[200010];
int main() {
    cin >> n >> m >> k;
    
    for (int i = 0; i < n; ++i) 
        cin >> v[i];
    
    sort(v, v + n);

    int a = -1, b = k;

    return 0;
}

