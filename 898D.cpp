#include <bits/stdc++.h>

using namespace std;

int n,m,k;
int v[100010];

int main(){
    cin >> n >> m >> k;
    for (int i = 0; i < n; ++i) {
        cin >> v[i];
    }
    sort(v,v + n);   

    return 0;
}

