#include <bits/stdc++.h>

using namespace std;

string a,b;
deque<char> q;
int main(){
    cin >> a >> b;
    int n = a.size();
    sort(a.begin(),b.end());
    sort(b.rbegin(),b.rend());
    int ca,cb; ca = cb = 0;
    bool flag = true;
    for (int i = 0; i < n; ++i){
        
    }
    return 0;
}


/*
cbxz
aaaa

abac
*/
